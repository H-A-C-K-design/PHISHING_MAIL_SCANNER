from setuptools import find_packages, setup

setup(
    name="phishing-email-detection",
    version="0.1.0",
    description="Phishing email detection project scaffold",
    packages=find_packages(where="."),
    include_package_data=True,
)
